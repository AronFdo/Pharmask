# FastAPI and web server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
jinja2>=3.1.3

# LangChain (app uses langchain-core + provider integrations only; not the umbrella `langchain` package)
langchain-core>=0.1.0
langchain-openai>=0.0.5
langchain-anthropic>=0.1.0
langchain-google-genai>=0.0.6
langchain-groq>=0.0.1

# Vector database (default embeddings use ONNX via chromadb; sentence-transformers not required)
chromadb>=0.4.22

# SQL database
sqlalchemy>=2.0.25

# Document processing
lxml>=5.1.0
pdfplumber>=0.10.3

# HTTP client
httpx>=0.26.0

# Configuration (.env loading is provided by pydantic-settings -> python-dotenv)
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Dataset download (scripts: download_pmc_dataset, pmc_pipeline, download_pubmedqa)
# Pulls huggingface-hub transitively
datasets>=2.16.0

# Evaluation / baselines
rank-bm25>=0.2.2
